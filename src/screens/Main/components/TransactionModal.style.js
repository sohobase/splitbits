import { StyleSheet } from 'react-native';
import { THEME } from '../../../config';

const { OFFSET } = THEME;

export default StyleSheet.create({

  option: {
    paddingLeft: OFFSET,
    paddingRight: OFFSET,
  },
});
