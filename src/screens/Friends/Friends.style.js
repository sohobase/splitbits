import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  screen: {
    backgroundColor: 'white',
  },

  input: {
    textAlign: 'left',
    paddingTop: 0,
    minWidth: '100%',
  },
});
